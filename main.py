import re

from deck import Deck

if __name__ == '__main__':

    deck = Deck(1)

    print(len(deck.deck))
    # hand = {1: deck.get_card(), 2: deck.get_card()}
    # test = {1: 'SEVEN_PI', 2: 'SEVEN_PI'}
    # print(hand, '  ', test)
    # deck = []
    # deck.append({'score': 2, 'suit': 'ch'})
    # print(deck[0]['score'])
    # hand = {1: 'SEVEN_PI', 2: 'SEVEN_PI'}
    # card = re.split('_', hand[1])
    # print(card)


    # card = deck.get_card()
    # print(card)
    # while deck.status:
    #     print(deck.get_card())








